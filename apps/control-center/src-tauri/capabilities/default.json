{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core bridge invoke + event listen",
  "windows": ["main"],
  "permissions": ["core:default"]
}
